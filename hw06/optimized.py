"""
Оптимизированная версия slow_code для вашего варианта.

Реализуйте функцию optimized_run(variant: int), которая
возвращает тот же результат, что и src/slow_code.VARIANTS[variant](),
но значительно быстрее (минимум 5x ускорение).
"""


def optimized_run(variant: int):
    """
    Запускает оптимизированную версию алгоритма для данного варианта.

    Должна возвращать результат, идентичный src/slow_code.VARIANTS[variant]().
    """
    raise NotImplementedError("Реализуй оптимизированную версию для своего варианта")
